name: tourism_recommenders_environment
channels:
  - defaults
dependencies:
  - pip=22
  - python=3
  - pip:
      - gitpython
      - jsonschema
      - meteostat
      - numpy
      - pandas
      - requests
      - semver
      - streamlit
      - toml
      - geocoder
      - geopy
